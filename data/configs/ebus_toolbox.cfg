# Configs for the eBus Toolbox

# Input file containing trip information
input = "./data/private_examples/trips_example-bvg_datetime.csv"
# output path for scenario.json
output = "./data/private_examples/scenario.json"
# Preferred charging type. Options: depot, opp (default: depot)
preferred_charging_type = "depot"
# vehicle types
vehicle_types = "./data/examples/vehicle_types.json"
# minimum standing time in depot for every bus in hours (default:6)
min_standing_time_depot = 5
#electrified stations
electrified_stations = data/private_examples/electrified_stations.json
# set random seed (for always random: set seed = null)
seed = 1
# set number of days for simulation (default: 7)
days = 2
# set length of timestep in minutes (default: 15 minute intervals)
interval = 1
# set minimum allowed state of charge when leaving (default: 0.8)
desired_soc = 1
# max power of charging station at depot and opp stations
cs_power_opp = 400
cs_power_depot = 100
# min charging time at opp station
min_charging_time_opp = 2
# define hours of traffic_jam (options are "stop&go" and "dense")
#rush_hour = {"stop&go": [["07:00", "09:00"], ["15:00", "17:00"]], "dense": [["17:00", "19:00"]]}

# set stationary batteries [capacity, C-Rate]
# battery with capacity = 75 kWh and C-Rate = 0.5
#battery = [[50, 1]]
# 2 batteries with capacity = 50 kWh, C-Rate = 1, grid_connector = "Betriebshof A")
battery = [[50, 1, "Betriebshof A"], [50 , 0.5, "Betriebshof Indira-Gandhi-Str."]]
# unlimited battery capacity with constant (dis)charge of 50 kWh
#battery = [[-1, 50]]


# set CSV-files
# external load timeseries (in this example in same directory as resulting json)
#include_ext_load_csv = Building-20210705-20210719.csv
# options for external load CSV:
# set CSV column name, timestep duration in sec. (example: 10 min) and factor (example: input in Watt)
#include_ext_csv_option = [["column", "value"], ["step_duration_s", 600], ["factor", 0.001], ["grid_connector_id", "Betriebshof A"]]

# local feed in timeseries (in this example in subdirectory "timeseries" - relative to resulting json)
#include_feed_in_csv = None #timeseries/feed_in.csv
# options for feed in CSV:
# set CSV column name, timestep duration in sec. (example: 1 hour) and factor (example: input in Watt)
#include_feed_in_csv_option = None # [["column", "value"], ["step_duration_s", 3600], ["factor", 1], ["grid_connector", "Betriebshof A"]]

# energy price timeseries in EUR/kWh (in this example in parent directory - relative to resulting json)
#include_price_csv = ../energy_price.csv
# options for price CSV: set CSV column name and timestep duration in sec. (example: 6 hours)
#include_price_csv_option = [["column", "value"], ["step_duration_s", 21600], ["grid_connector", "Betriebshof A"]]
# for more options, see https://github.com/rl-institut/spice_ev/wiki/Command-line-options#csv-file-options
